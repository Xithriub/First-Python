class Time:
	def __init__(self,hour,minutes):
		self.hour=hour
		self.minutes=minutes
	def combi(self):
		obsh=self.hour+self.minutes
		return obsh
	def operation(self):
		retry=self.hour+self.minutes
		fass=retry+obsh
		return retry-fass
	def solo(self):
		q=[]
		for i in self.hour:
			if i%2==0:
				q.append(i)
	def 

